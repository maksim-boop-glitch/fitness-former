services:
  - type: web
    name: fitness-former
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server/index.js
    branch: main
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
